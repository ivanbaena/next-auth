const HomePageContent = () => {
  // Show Link to Login page if NOT auth

  return (
    <section className="flex items-center justify-center min-h-screen">
      <h1 className="text-6xl">Welcome on Board!</h1>
    </section>
  );
};

export default HomePageContent;
